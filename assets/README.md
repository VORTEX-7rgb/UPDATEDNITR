# Assets Directory

Place the following 3 screenshot images here to display them in the main `README.md`:

1. `dashboard.jpg` (or `.png`) — Screenshot of the main bot menu showing Next Class, Attendance status, and buttons.
2. `status.jpg` (or `.png`) — Screenshot of the `/status` system telemetry message.
3. `registration_alert.jpg` (or `.png`) — Screenshot of the admin registration alert notification.
